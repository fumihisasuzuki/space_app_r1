require 'rails_helper'

RSpec.describe "Events", type: :request do
  describe "GET /events/new_a" do
    it "works! (now write some real specs)" do
#      get events_new_a_path
#      expect(response).to have_http_status(200)
    end
  end
end
