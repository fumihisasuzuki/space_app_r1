＜<%= l(schedule.held_at, format: :shortdate) %>(<%= $days_of_the_week[schedule.held_at.wday] %>)<%= event.event_name %>　最終のご案内＞
来る<%= l(schedule.held_at, format: :shortdate) %>(<%= $days_of_the_week[schedule.held_at.wday] %>)に予定しております<%= event.event_name %>の詳細が決定しましたので、下記の通りご案内致します。
当日皆様がお越しになるのを楽しみにしております。

記

日時：<%= l(schedule.held_at, format: :longdate) %>(<%= $days_of_the_week[schedule.held_at.wday] %>) <%= l(schedule.held_at, format: :time) %>~
場所：<%= shop.shop_name %><%= "（最寄り駅：#{event.station}駅）" if event.station.present? %><%= "
お店HP：#{shop.shop_url}" if shop.shop_url.present? %><%= "
お店住所：#{shop.shop_address}" if shop.shop_address.present? %>
会費：<%= event.indication_price.present? ? event.indication_price : "実費" %>
集金：<%= event.indication_price.present? ? "会の開始前までに私までお願いします" : "清算後現地回収" %>
集合：お時間の5~10分前に直接お店までお願いします。
<% if event.chouseisan_check? %>
  下記のURLから皆様の回答状況を確認できます。
  日程調整用URL：<%= event.chouseisan_url %>
<% end %>

以上