＜<%= event.event_name %>　日程決定のご連絡＞
下記URLにて日程調整しておりました<%= event.event_name %>の開催日が決定しましたのでご連絡致します。
詳細につきましては、後日改めてご連絡致しますので、よろしくお願い申し上げます。

記

日時：<%= l(schedule.held_at, format: :longdate) %>(<%= $days_of_the_week[schedule.held_at.wday] %>) <%= l(schedule.held_at, format: :time) %>~
場所：<%= event.station %>駅周辺<%= "の#{event.place}を予定" if event.place.present? %>
お店：未定
会費：未定
<% if event.chouseisan_check? %>
  下記のURLから皆様の回答状況を確認できます。
  日程調整用URL：<%= event.chouseisan_url %>
<% end %>

以上