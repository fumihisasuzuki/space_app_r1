<% provide(:title, 'Resend unlock') %>
<div class="container">
  <h2>ロック解除メール再送付</h2>

  <div class="col-md-4 col-md-offset-4">
    <h5>アカウントロック解除用のメールが届かなかった方は、お手数ですが、正しいメールアドレスを入力してください。</h5>
    <h5>Gmailドメイン（@gmail.com）を受け取れるよう設定確認の上、迷惑メールボックスに入っていないか、合わせてご確認ください。</h5>

    <%= form_for(resource, as: resource_name, url: unlock_path(resource_name), html: { method: :post }) do |f| %>
      <%= render "devise/shared/error_messages", resource: resource %>
    
      <div class="field">
        <%= f.label :email %><br />
        <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control" %>
      </div>
    
      <div class="actions">
        <%= f.submit "ロック解除リンクを再送付", class: "btn btn-primary btn-block btn-user--edit" %>
      </div>
    <% end %>
    
    <%= render "users/shared/links" %>

  </div>
</div>